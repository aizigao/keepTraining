#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char *toChar(char *str)
{
  return str;
}

int add(int x, int y)
{
  return x + y;
}

int square(int x)
{
  return x * x;
}
int test(int x, int y)
{
  return log(x);
}